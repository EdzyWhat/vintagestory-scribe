{
  "version": 1,
  "file": "scribe-client-config.json",
  "settings": [
    {
      "code": "VisibleListHeight",
      "type": "float",
      "default": 400,
      "comment": "Fixed viewport height for the lectern's scrollable row-list region.",
      "range": { "min": 100, "max": 1000 }
    },
    {
      "code": "RowSpacing",
      "type": "float",
      "default": 14,
      "comment": "Vertical gap between rows in the lectern's row list.",
      "range": { "min": 0, "max": 60 }
    },
    {
      "code": "TopContentGap",
      "type": "float",
      "default": 20,
      "comment": "Vertical gap between the lectern's title bar and its first row.",
      "range": { "min": 0, "max": 100 }
    },
    {
      "code": "ReadListWidth",
      "type": "float",
      "default": 300,
      "comment": "Row-list width in the lectern's read view.",
      "range": { "min": 100, "max": 800 }
    },
    {
      "code": "EditorListWidth",
      "type": "float",
      "default": 340,
      "comment": "Row-list width in the lectern's editor view.",
      "range": { "min": 100, "max": 800 }
    },
    {
      "code": "RowDividerThickness",
      "type": "float",
      "default": 2,
      "comment": "Thickness, in pixels, of the divider line drawn below each lectern row.",
      "range": { "min": 0, "max": 10 }
    },
    {
      "code": "RowDividerBrightness",
      "type": "float",
      "default": 0.85,
      "comment": "Brightness (0-1) of the divider line drawn below each lectern row.",
      "range": { "min": 0, "max": 1 }
    }
  ]
}
